class Solution {
public:
    int mirrorReflection(int p, int q) {
        int g = __gcd(p, q);
        p /= g;
        q /= g;

        if (p % 2 == 0 && q % 2 == 1) return 2;
        if (p % 2 == 1 && q % 2 == 1) return 1;
        return 0;
    }
};

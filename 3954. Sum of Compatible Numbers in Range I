class Solution {
public:
    long long sumOfCompatibleNumbers(int n, int k) {
        long long ans = 0;

        for (int x = max(1, n - k); x <= n + k; x++) {
            if ((n & x) == 0) {
                ans += x;
            }
        }

        return ans;
    }
};
